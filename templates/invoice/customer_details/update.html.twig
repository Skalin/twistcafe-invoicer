{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        <h1>Update customer ID: {{ model.id }}</h1>

        <div>
            <div class="row">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}